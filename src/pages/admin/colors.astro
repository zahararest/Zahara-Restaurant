---
// ─────────────────────────────────────────────────────────────────────────────
//
// Architecture notes
// ──────────────────
// • Token groups + Token definitions live here (server-side) because Astro
//   uses them to render the HTML row elements. This is the correct Astro
//   pattern for static server-rendered data.
//
// • PRESETS are defined inside the <script> tag below — they are purely
//   client-side data. Keeping them here in the frontmatter and passing them
//   via define:vars was unnecessary. Only `defaults` (the token→hex map) is
//   passed via define:vars because it is derived from the array above.
//
// • STAGING MODEL: applyToken() applies to :root immediately so the preview
//   updates live, but does NOT write to localStorage or broadcast to other
//   tabs. Only clicking "Save" (or ⌘S / Ctrl+S) persists changes. "Discard"
//   reverts all unsaved changes.

import '../../styles/admin-colors.css';
import { groups, defaults, darkDefaults } from '../../data/admin-colors';

// Admin is single-rooted at /admin (the venue is chosen by the site-switch
// cookie, not the URL). The dual-base build would otherwise emit an orphan copy
// at /rooftop/admin/colors — redirect it back to the canonical one.
if (import.meta.env.BASE_URL.startsWith('/rooftop')) {
  return Astro.redirect('/admin/colors/');
}

const TITLE = 'Color editor · Zahara';
// Presets live in the <script> below — no define:vars needed for them.
---

<!doctype html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="noindex, nofollow" />
  <title>{TITLE}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" />

</head>
<body>

<header class="editor-top">
  <nav class="editor-top__nav" aria-label="Admin sections">
    <a class="editor-top__brand" href="/admin/"><span class="editor-top__venuename" data-venue-name>Zahara</span> · Admin</a>
    <a class="editor-top__navlink"           href="/admin/">Menu editor</a>
    <a class="editor-top__navlink"           href="/admin/images/">Images</a>
    <a class="editor-top__navlink"           href="/admin/content/">Content</a>
    <a class="editor-top__navlink is-active" href="/admin/colors/" aria-current="page">Colors</a>
    <span class="editor-top__spacer"></span>
    {/* Venue switch — mirrors the shared admin topbar; scopes which venue's
        palette this editor loads + saves (via the zahara_admin_site cookie). */}
    <div class="editor-venue" role="group" aria-label="Venue being edited">
      <span style="font-size:.62rem;letter-spacing:.18em;text-transform:uppercase;font-weight:700;color:var(--e-mut);margin-inline-end:.4rem;">Editing</span>
      <button type="button" data-venue="zahara">Zahara</button>
      <button type="button" data-venue="rooftop">Rooftop</button>
    </div>
    <a class="editor-top__site" href="/en/" target="_blank">View site ↗</a>
  </nav>
  <script is:inline>(function(){
    var CK='zahara_admin_site';
    function cur(){var m=document.cookie.match(/(?:^|;\s*)zahara_admin_site=(rooftop|zahara)/);return m?m[1]:'zahara';}
    var now=cur();
    // Tint the whole editor for the venue being edited (stripe + accent), and
    // name it in the brand lockup — same signal as every other admin page.
    document.documentElement.setAttribute('data-venue', now);
    var nameEl=document.querySelector('[data-venue-name]');
    if(nameEl) nameEl.textContent = now==='rooftop' ? 'Nucha Rooftop' : 'Zahara';
    document.title = (now==='rooftop' ? 'Nucha Rooftop' : 'Zahara') + ' · Colors · Admin';
    document.querySelectorAll('.editor-venue [data-venue]').forEach(function(b){
      var v=b.getAttribute('data-venue');
      var on=v===now;
      var venue=now==='rooftop'?'#1F6260':'#9C4621';
      b.style.cssText='font:inherit;font-size:.72rem;letter-spacing:.1em;text-transform:uppercase;font-weight:700;padding:.35rem .8rem;cursor:pointer;border:1px solid '+venue+';background:'+(on?venue:'transparent')+';color:'+(on?'#fff':'#6f6457')+';';
      b.addEventListener('click',function(){
        if(v===cur())return;
        document.cookie=CK+'='+v+';path=/admin;max-age=31536000;samesite=lax';
        location.reload();
      });
    });
  })();</script>

  <div class="editor-top__tools">
    <h1>Palette editor</h1>

    <input type="search" class="top-search" data-search
           placeholder="Search tokens…" aria-label="Search colour tokens" />

    <select class="top-select" data-preset aria-label="Load a preset theme">
      <option value="">Load preset…</option>
      <optgroup label="Light themes">
        <option value="default">Default (warm cream)</option>
        <option value="monochrome">Monochrome</option>
        <option value="terracotta">Terracotta</option>
        <option value="forest">Forest</option>
        <option value="navy-light">Navy (light)</option>
        <option value="linen">Linen (crisp pale + navy)</option>
        <option value="rose-paper">Rose paper (wine accent)</option>
        <option value="sage">Sage (pale green + moss)</option>
        <option value="olive-gold">Olive &amp; Gold (green + gold)</option>
        <option value="sea-glass">Sea Glass (aqua-green + teal)</option>
        <option value="fern-copper">Fern &amp; Copper (green + copper)</option>
        <option value="honey-sage">Honey &amp; Sage (green + honey gold)</option>
        <option value="lavender-haze">Lavender Haze (light purple)</option>
        <option value="lilac-sage">Lilac &amp; Sage (purple + green)</option>
        <option value="wisteria">Wisteria (lilac + blue-violet)</option>
        <option value="amethyst-cream">Amethyst &amp; Cream (purple + gold)</option>
        <option value="blush">Blush (soft rose)</option>
        <option value="harbor">Harbor (blue-gray + slate-teal)</option>
      </optgroup>
      <optgroup label="Dark themes">
        <option value="midnight">Midnight (dark navy)</option>
        <option value="obsidian">Obsidian (near-black + gold)</option>
        <option value="espresso">Espresso (dark brown)</option>
        <option value="plum">Plum (dark purple)</option>
        <option value="dusk-navy">Dusk Navy (cool slate)</option>
        <option value="charcoal">Charcoal (warm gray + rust)</option>
        <option value="moss">Moss (dark green + gold)</option>
        <option value="wine">Wine (deep aubergine)</option>
        <option value="cobalt-noir">Cobalt Noir (ink + electric blue)</option>
        <option value="emerald-noir">Emerald Noir (green + gold)</option>
        <option value="pine-gold">Pine &amp; Gold (green + gold)</option>
        <option value="deep-jade">Deep Jade (teal-green)</option>
        <option value="teal-abyss">Teal Abyss (electric teal)</option>
        <option value="violet-dusk">Violet Dusk (purple + violet)</option>
        <option value="orchid-noir">Orchid Noir (plum + orchid)</option>
        <option value="amethyst-dark">Amethyst Dark (purple + gold)</option>
        <option value="indigo-night">Indigo Night (indigo + periwinkle)</option>
        <option value="rosewood">Rosewood (rose-brown + pink)</option>
        <option value="dark-frantzen">Dark · Frantzen (blue)</option>
        <option value="dark-white">Dark + white (stark)</option>
      </optgroup>
    </select>

    <div class="mode-toggle" role="group" aria-label="Edit light or dark palette">
      <button type="button" class="mode-toggle__btn is-active" data-mode-btn="light" aria-pressed="true">☀ Light</button>
      <button type="button" class="mode-toggle__btn" data-mode-btn="dark" aria-pressed="false">☾ Dark</button>
    </div>

    <label class="top-toggle-label">
      <input type="checkbox" data-modified-only />
      Modified only
    </label>

    <span class="editor-top__spacer"></span>

    <span class="pending-badge" data-pending-badge hidden>0 unsaved</span>
    <button class="btn btn-top btn-top--ghost btn-top--discard" type="button"
            data-discard disabled title="Discard all unsaved changes">Discard</button>
    <button class="btn btn-top btn-top--save" type="button"
            data-save disabled title="Save changes to localStorage &amp; broadcast (⌘S)">
      Save<span class="kbd">⌘S</span>
    </button>
    <button class="btn btn-top btn-top--ghost" type="button" data-undo
            title="Undo (⌘Z)" disabled>↶ Undo</button>
    <button class="btn btn-top btn-top--ghost" type="button" data-reset>Reset all</button>
    <button class="btn btn-top" type="button" data-export>Export CSS</button>
  </div>
</header>

<main class="layout">
  <!-- ── Token list (left pane) ─────────────────────────────── -->
  <section class="side" aria-label="Colour token editors">
    <h2>Tokens</h2>
    <p class="side__intro">
      Edits preview live but aren't saved until you click <strong>Save</strong>
      (<span class="kbd">⌘S</span> / <span class="kbd">Ctrl+S</span>).
      <span class="kbd">▣</span> picks a colour · <span class="kbd">👁</span>
      highlights where it's used.
    </p>

    {groups.map((g) => (
      <div class="group" data-group>
        <div class="group__head" data-group-toggle>
          <span class="group__toggle" aria-hidden="true">▾</span>
          <div class="group__head-text">
            <h3 class="group__title">{g.group}</h3>
            <p class="group__intro" set:html={g.intro} />
          </div>
        </div>
        <div class="group__body">
          {g.tokens.map((t) => (
            <div class="row"
                 data-row
                 data-token={t.token}
                 data-short={t.shortName}
                 data-default={t.def}
                 data-default-dark={darkDefaults[t.token] || t.def}
                 data-search-key={(t.label + ' ' + t.token + ' ' + (t.hint || '')).toLowerCase()}>
              <div class="row__swatch-area">
                <span class="row__dot" data-modified-dot title="Modified from default"></span>
                <input type="color" value={t.def} data-picker aria-label={`${t.label} colour picker`} />
              </div>

              <div class="row__meta">
                <div class="row__name">
                  {t.label}
                  <span class="row__contrast" data-contrast aria-label="Contrast ratio"></span>
                </div>
                {t.hint && <div class="row__hint">{t.hint}</div>}
                <div class="row__token-name"><code>{t.token}</code></div>
              </div>

              <div class="row__controls">
                <input type="text" class="row__hex" data-hex value={t.def}
                       maxlength="7" inputmode="text" autocomplete="off"
                       spellcheck="false" aria-label={`${t.label} hex value`} />
                <button class="row__btn row__btn--show" type="button" data-show
                        title="Highlight affected preview elements (no value change)"
                        aria-label="Highlight preview">👁</button>
                <button class="row__btn" type="button" data-palette-btn
                        title="Pick from current palette" aria-label="Pick from palette">▣</button>
                <button class="row__btn" type="button" data-copy title="Copy hex">⧉</button>
                <button class="row__btn" type="button" data-reset-one title="Reset to default">↺</button>
              </div>
            </div>
          ))}
        </div>
      </div>
    ))}
  </section>

  <!-- ── Preview pane (right pane) ──────────────────────────── -->
  <aside class="preview-pane" aria-label="Live site preview">
    <h2>Preview — EN site</h2>
    <p class="preview-pane__intro">
      Every block mirrors a real moment on the public English site.
      Editing a token pulses the fragments that use it.
    </p>

    <div class="sim" role="img" aria-label="Site colour preview">

      <!-- SCROLL PROGRESS BAR -->
      <div class="sim__progress preview-fragment" data-uses="accent,gold"
           title="Scroll progress bar — uses --accent and --gold"></div>

      <!-- NON-PHOTO HEADER (header on /menu/, /contact/, /location/) -->
      <div class="sim__nav preview-fragment" data-uses="paper,rule-soft">
        <span class="sim__nav-brand preview-fragment" data-uses="ink">ZAHARA</span>
        <div class="sim__nav-links">
          <span class="preview-fragment" data-uses="ink-soft">HOME</span>
          <span class="preview-fragment" data-uses="ink-soft">LOCATION</span>
          <span class="preview-fragment" data-uses="ink,accent">MENU</span>
        </div>
        <span class="sim__nav-reserve preview-fragment" data-uses="ink">Reserve</span>
      </div>

      <!-- PAGE surface -->
      <div class="sim__page preview-fragment" data-uses="paper">

        <!-- HERO with floating photo header -->
        <div class="sim__hero">
          <div class="sim__hero-bar">
            <span class="sim__hero-brand preview-fragment" data-uses="paper-on-photo">ZAHARA</span>
            <span class="sim__hero-navs preview-fragment" data-uses="paper-on-photo">HOME · LOCATION · MENU · CONTACT</span>
            <span class="sim__hero-reserve preview-fragment" data-uses="paper-on-photo">Reserve</span>
          </div>
          <div class="sim__hero-body">
            <p class="sim__hero-eyebrow preview-fragment" data-uses="paper-on-photo,accent">
              CHEF RESTAURANT · JERUSALEM
            </p>
            <h3 class="sim__hero-title preview-fragment" data-uses="paper-on-photo">
              A new kitchen, on Ben Sira.
            </h3>
            <div class="sim__hero-btns">
              <span class="sim__hero-btn preview-fragment" data-uses="ink,paper">Reserve a table</span>
              <span class="sim__hero-btn ghost preview-fragment" data-uses="paper-on-photo">See the menu</span>
            </div>
          </div>
        </div>

        <!-- INFO STRIP -->
        <div class="sim__info-strip preview-fragment" data-uses="paper-deep,rule-soft">
          <div class="sim__info-cell">
            <span class="preview-fragment" data-uses="ink-muted">HOURS</span>
            <strong class="preview-fragment" data-uses="ink">Mon–Thu · 18:00–23:00</strong>
          </div>
          <div class="sim__info-cell">
            <span class="preview-fragment" data-uses="ink-muted">RESERVE</span>
            <strong class="preview-fragment" data-uses="ink">077-303-4180</strong>
          </div>
        </div>

        <!-- ISSUE STAMP -->
        <div class="sim__issue preview-fragment" data-uses="paper-deep,ink-muted,rule-soft">
          <span>VOLUME I · JERUSALEM · 2026</span>
          <span class="sim__issue-rule preview-fragment" data-uses="accent"></span>
          <span class="serif preview-fragment" data-uses="ink-soft">From the kitchen of Roi Achdut</span>
        </div>

        <!-- EDITORIAL SECTION -->
        <div class="sim__article">
          <p class="sim__eyebrow preview-fragment" data-uses="ink-muted,accent">THE STORY</p>
          <h3 class="sim__title preview-fragment" data-uses="ink">A new chapter, one kitchen.</h3>
          <p class="sim__body preview-fragment" data-uses="ink-soft">
            Roi cooks what arrived from the market that morning — sea bass,
            blistered tomatoes, a leaf salad. The menu shifts weekly.
            Wine list short, considered.
          </p>
          <div class="sim__btn-row">
            <button class="sim__btn preview-fragment" type="button" data-uses="ink,paper">Reserve</button>
            <button class="sim__btn ghost preview-fragment" type="button" data-uses="ink">Menu</button>
          </div>
        </div>

        <!-- MENU TABS (shows --ink as active-tab background) -->
        <div class="sim__tabs preview-fragment" data-uses="paper,rule-soft">
          <span class="sim__tab is-active preview-fragment" data-uses="ink,paper-on-photo">Food</span>
          <span class="sim__tab preview-fragment" data-uses="ink-muted">Cocktails</span>
          <span class="sim__tab preview-fragment" data-uses="ink-muted">Wine</span>
          <span class="sim__tab preview-fragment" data-uses="ink-muted">Desserts</span>
          <span class="sim__tabs-indicator preview-fragment" data-uses="accent"></span>
        </div>

        <!-- STRONG RULE -->
        <div class="sim__rule preview-fragment" data-uses="rule"></div>

        <!-- MENU SECTION CARD — how the /menu/ page renders each section:
             a paper-card gradient with a serif heading + accent underline,
             ink item names, ink-soft descriptions, accent prices and a soft
             gold corner ornament. -->
        <div class="sim__menucard preview-fragment" data-uses="paper-card,paper,rule-soft,gold,accent">
          <h4 class="sim__menucard-title preview-fragment" data-uses="ink,accent">Something to start</h4>
          <div class="sim__menucard-row preview-fragment" data-uses="rule-soft">
            <div class="sim__menucard-main">
              <strong class="preview-fragment" data-uses="ink">Bread service</strong>
              <span class="sim__menucard-desc preview-fragment" data-uses="ink-soft">sourdough, cultured butter, olive oil</span>
            </div>
            <span class="preview-fragment" data-uses="accent">32</span>
          </div>
          <div class="sim__menucard-row preview-fragment" data-uses="rule-soft">
            <div class="sim__menucard-main">
              <strong class="preview-fragment" data-uses="ink">Beef tartare</strong>
              <span class="sim__menucard-desc preview-fragment" data-uses="ink-soft">smoked egg yolk, capers, toast</span>
            </div>
            <span class="preview-fragment" data-uses="accent">78</span>
          </div>
          <div class="sim__menucard-row preview-fragment" data-uses="rule-soft">
            <div class="sim__menucard-main">
              <strong class="preview-fragment" data-uses="ink">Sea-bass sashimi</strong>
            </div>
            <span class="preview-fragment" data-uses="accent">78</span>
          </div>
        </div>

        <!-- SOFT RULE -->
        <div class="sim__rule sim__rule--soft preview-fragment" data-uses="rule-soft"></div>

        <!-- PULL QUOTE (accent-soft bg, gold italic) -->
        <blockquote class="sim__quote preview-fragment" data-uses="accent-soft">
          <span class="preview-fragment" data-uses="ink">"We don't cook plates. We cook the </span>
          <em class="preview-fragment" data-uses="gold">evening</em>
          <span class="preview-fragment" data-uses="ink"> for people."</span>
        </blockquote>

        {/* HOME MENU TILES — the food / wine / cocktails tiles in the home
            "menu" section. Each text piece binds to its own --tile-* token.
            The tiles sit over a dark photo in both themes, so the text stays
            light independent of the rest of the page palette. */}
        <div class="sim__tiles">
          <div class="sim__tile">
            <span class="sim__tile-num preview-fragment" data-uses="tile-num">01</span>
            <span class="sim__tile-label preview-fragment" data-uses="tile-label">Food</span>
          </div>
          <div class="sim__tile sim__tile--open">
            <span class="sim__tile-eyebrow preview-fragment" data-uses="tile-num">Chef’s picks</span>
            <span class="sim__tile-fave preview-fragment" data-uses="tile-fave">Beef tartare</span>
            <span class="sim__tile-fave preview-fragment" data-uses="tile-fave">Sea-bass sashimi</span>
            <span class="sim__tile-link preview-fragment" data-uses="tile-link">See full menu ↗</span>
          </div>
          <div class="sim__tile">
            <span class="sim__tile-num preview-fragment" data-uses="tile-num">02</span>
            <span class="sim__tile-label preview-fragment" data-uses="tile-label">Wine</span>
          </div>
        </div>

        <!-- TOC -->
        <div class="sim__toc">
          <p class="sim__eyebrow preview-fragment" data-uses="ink-muted,accent">ON THE MENU TONIGHT</p>
          <div class="sim__toc-row">
            <span class="preview-fragment" data-uses="accent">01</span>
            <span class="preview-fragment" data-uses="ink">Something to start</span>
            <span class="sim__toc-dots preview-fragment" data-uses="ink-faint"></span>
            <span class="preview-fragment" data-uses="ink-soft">Openings from the kitchen</span>
          </div>
          <div class="sim__toc-row">
            <span class="preview-fragment" data-uses="accent">02</span>
            <span class="preview-fragment" data-uses="ink">Almost there</span>
            <span class="sim__toc-dots preview-fragment" data-uses="ink-faint"></span>
            <span class="preview-fragment" data-uses="ink-soft">Fish and meat from the market</span>
          </div>
        </div>

        <!-- CATEGORY CHIPS -->
        <div style="padding:0.35rem 0">
          <p class="sim__eyebrow preview-fragment" data-uses="ink-muted,accent" style="padding:0 0.9rem;margin-bottom:0.5rem">SECTION ACCENTS</p>
          <div class="sim__chips preview-fragment" data-uses="paper">
            <span class="sim__chip sim__chip--green preview-fragment" data-uses="c-green">Food</span>
            <span class="sim__chip sim__chip--teal  preview-fragment" data-uses="c-teal">Cocktails</span>
            <span class="sim__chip sim__chip--slate preview-fragment" data-uses="c-slate">Wine</span>
            <span class="sim__chip sim__chip--mauve preview-fragment" data-uses="c-mauve">Desserts</span>
            <span class="sim__chip sim__chip--gold  preview-fragment" data-uses="gold">Highlight</span>
          </div>
        </div>

        <!-- CONTACT TOGGLE -->
        <div class="sim__toggle preview-fragment" data-uses="paper-card,rule">
          <span class="sim__toggle-btn is-active preview-fragment" data-uses="ink">General inquiry</span>
          <span class="sim__toggle-btn preview-fragment" data-uses="ink-muted">Event inquiry</span>
          <span class="sim__toggle-ind preview-fragment" data-uses="accent"></span>
        </div>

        <!-- REACH-US CARD -->
        <div class="sim__reach preview-fragment" data-uses="paper-card,rule-soft" style="margin-top:0.5rem">
          <p class="sim__eyebrow preview-fragment" data-uses="ink-muted,accent">OTHER WAYS TO REACH US</p>
          <div class="sim__reach-row">
            <span class="preview-fragment" data-uses="ink-muted">PHONE</span>
            <a class="sim__reach-link preview-fragment" data-uses="ink,accent,accent-deep">077-303-4180</a>
          </div>
          <div class="sim__reach-row">
            <span class="preview-fragment" data-uses="ink-muted">EMAIL</span>
            <a class="sim__reach-link preview-fragment" data-uses="ink,accent,accent-deep">hello@zahara.rest</a>
          </div>
        </div>

        <!-- SELECTION + FOCUS RING demos -->
        <div class="sim__selection preview-fragment" data-uses="ink">
          Normal text and
          <mark class="sim__sel-mark preview-fragment" data-uses="accent-soft,ink">selected text highlight</mark>
          back to normal.
        </div>
        <div class="sim__focus-demo">
          <span class="preview-fragment" style="font-size:0.62rem;letter-spacing:0.1em;text-transform:uppercase;font-family:'Inter',sans-serif;color:var(--ink-muted,#8A7E68);">Focus ring →</span>
          <span class="sim__focus-ring preview-fragment" data-uses="accent,rule,ink">focused element</span>
        </div>

        <!-- SCROLLBAR demo -->
        <div class="sim__scrollbar">
          <div class="sim__sb-track preview-fragment" data-uses="paper-deep,rule-soft">
            <div class="sim__sb-thumb preview-fragment" data-uses="accent,accent-deep"></div>
          </div>
          <span class="sim__sb-label preview-fragment" data-uses="ink-muted">Scrollbar</span>
        </div>

        <!-- FAINT METADATA -->
        <p class="sim__faint preview-fragment" data-uses="ink-faint">
          Last updated · 12 March 2026 · Zahara Restaurant
        </p>

        {/* FLOATING UI — same three viewport-pinned widgets the live
            site shows in its bottom corners and inline-end edge. The
            preview lays them out inline (not pinned) so they sit next
            to the rest of the simulated sections; tokens consumed are
            exactly the ones the real elements use. */}
        <div class="sim__floating">
          {/* Vertical scroll rail (E) — one tick per page section.
              Active tick fills with --accent and the label fades in. */}
          <div class="sim__rail" aria-label="Scroll rail preview">
            <span class="sim__rail-tick preview-fragment" data-uses="ink-muted">
              <span class="sim__rail-mark preview-fragment" data-uses="ink-muted"></span>
              <span class="sim__rail-label preview-fragment" data-uses="ink-muted">OPEN</span>
            </span>
            <span class="sim__rail-tick is-active preview-fragment" data-uses="ink,accent">
              <span class="sim__rail-mark preview-fragment" data-uses="accent"></span>
              <span class="sim__rail-label preview-fragment" data-uses="ink">STORY</span>
            </span>
            <span class="sim__rail-tick preview-fragment" data-uses="ink-muted">
              <span class="sim__rail-mark preview-fragment" data-uses="ink-muted"></span>
              <span class="sim__rail-label preview-fragment" data-uses="ink-muted">KITCHEN</span>
            </span>
            <span class="sim__rail-tick preview-fragment" data-uses="ink-muted">
              <span class="sim__rail-mark preview-fragment" data-uses="ink-muted"></span>
              <span class="sim__rail-label preview-fragment" data-uses="ink-muted">MENU</span>
            </span>
            <span class="sim__rail-tick preview-fragment" data-uses="ink-muted">
              <span class="sim__rail-mark preview-fragment" data-uses="ink-muted"></span>
              <span class="sim__rail-label preview-fragment" data-uses="ink-muted">GALLERY</span>
            </span>
          </div>

          {/* Bottom-corner buttons — accessibility (inline-start) and
              scroll-to-top (inline-end). Both share the same
              ink-on-paper-on-photo treatment. */}
          <div class="sim__corner-row">
            <div class="sim__corner-cell">
              <span class="sim__corner-btn preview-fragment" data-uses="ink,paper">♿</span>
              <span class="sim__corner-caption preview-fragment" data-uses="ink-muted">A11Y TOGGLE</span>
            </div>
            <div class="sim__corner-cell">
              <span class="sim__corner-btn preview-fragment" data-uses="ink,paper">↑</span>
              <span class="sim__corner-caption preview-fragment" data-uses="ink-muted">SCROLL-TO-TOP</span>
            </div>
          </div>
        </div>

        <!-- FORM STATUS -->
        <div class="sim__status-row">
          <div class="sim__status sim__status--ok preview-fragment" data-uses="ok">
            ✓ Thanks — we'll be in touch within one business day.
          </div>
          <div class="sim__status sim__status--err preview-fragment" data-uses="err">
            ✗ Network error. Please try again.
          </div>
        </div>

        <!-- FOOTER -->
        <div class="sim__footer preview-fragment" data-uses="paper-deep,rule-soft">
          <div class="sim__footer-grid">
            <div class="sim__footer-block">
              <span class="sim__footer-label preview-fragment" data-uses="ink-muted">HOURS</span>
              <span class="sim__footer-val preview-fragment" data-uses="ink">Mon–Thu 18:00–23:00</span>
            </div>
            <div class="sim__footer-block">
              <span class="sim__footer-label preview-fragment" data-uses="ink-muted">ADDRESS</span>
              <span class="sim__footer-val preview-fragment" data-uses="ink">Ben Sira 6, Jerusalem</span>
            </div>
            <div class="sim__footer-block">
              <span class="sim__footer-label preview-fragment" data-uses="ink-muted">PHONE</span>
              <a class="sim__footer-val preview-fragment" data-uses="ink,accent">077-303-4180</a>
            </div>
            <div class="sim__footer-block">
              <span class="sim__footer-label preview-fragment" data-uses="ink-muted">RESERVE</span>
              <a class="sim__footer-val preview-fragment" data-uses="ink,accent">Book a table</a>
            </div>
          </div>
          <div class="sim__footer-bottom preview-fragment" data-uses="ink-muted,rule-soft">
            <span class="preview-fragment" data-uses="ink-muted">© 2026 Zahara Restaurant</span>
            <span class="preview-fragment" data-uses="ink-muted">Accessibility · Privacy</span>
          </div>
        </div>

      </div><!-- /sim__page -->
    </div><!-- /sim -->
  </aside>
</main>

<!-- Status toast -->
<div class="toast" data-toast role="status" aria-live="polite"></div>

<!-- Palette popup (shared, repositioned by JS) -->
<div class="palette-popup" hidden data-palette-popup role="listbox" aria-label="Pick from current palette">
  <p class="palette-popup__title">Current palette</p>
  <div class="palette-popup__grid" data-palette-grid></div>
</div>

<!-- Export modal -->
<div class="export-modal" hidden data-export-modal>
  <div class="export-modal__inner">
    <h2>Export CSS</h2>
    <p>Paste this <code>:root</code> block into <code>src/styles/global.css</code> to make the palette permanent.</p>
    <div class="export-modal__tabs">
      <button class="export-tab is-active" type="button" data-export-tab="full">Full palette</button>
      <button class="export-tab" type="button" data-export-tab="changed">Changed only</button>
    </div>
    <textarea data-export-textarea readonly></textarea>
    <div class="export-modal__row">
      <button class="btn btn-top btn-top--ghost" type="button" data-export-close>Close</button>
      <button class="btn btn-top" type="button" data-export-copy>Copy to clipboard</button>
    </div>
  </div>
</div>

<script is:inline define:vars={{ defaults, darkDefaults }}>
(function () {
  'use strict';

  // ────────────────────────────────────────────────────────────────────────────
  // PRESETS — defined here in the client script, not in the Astro frontmatter.
  // They are purely static client-side data; no server processing is needed.
  // ────────────────────────────────────────────────────────────────────────────
  const PRESETS = {

    // ── Light themes ──────────────────────────────────────────────────────────

    default: defaults,

    monochrome: {
      '--paper': '#F4F1EB', '--paper-deep': '#E8E3D8', '--paper-edge': '#D4CCBE',
      '--paper-card': '#FAF7F1', '--paper-on-photo': '#F2EFE7',
      '--ink': '#15110D', '--ink-soft': '#34302A', '--ink-muted': '#807868', '--ink-faint': '#B0A793',
      '--rule': '#C8BFAA', '--rule-soft': '#DCD3BD',
      '--accent': '#2F2A22', '--accent-deep': '#15110D', '--accent-soft': '#D8D2C0',
      '--gold': '#8A7E68',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0E0C0A',
    },

    terracotta: {
      '--paper': '#F4EDDF', '--paper-deep': '#ECE3D0', '--paper-edge': '#DFD4BB',
      '--paper-card': '#FBF7EE', '--paper-on-photo': '#F8F2E5',
      '--ink': '#1A1410', '--ink-soft': '#3D362E', '--ink-muted': '#857C6C', '--ink-faint': '#B6AC97',
      '--rule': '#D5CBB1', '--rule-soft': '#E5DCC4',
      '--accent': '#9C4621', '--accent-deep': '#6F2F12', '--accent-soft': '#F2DFCF',
      '--gold': '#B68A3F',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0E0805',
    },

    forest: {
      '--paper': '#F0EDE3', '--paper-deep': '#E2DFD2', '--paper-edge': '#CDCBB8',
      '--paper-card': '#F8F5EB', '--paper-on-photo': '#F2EFE3',
      '--ink': '#16181A', '--ink-soft': '#2E3431', '--ink-muted': '#6E7A72', '--ink-faint': '#A3AC9F',
      '--rule': '#BFC6B7', '--rule-soft': '#D6DAC8',
      '--accent': '#3F6B4B', '--accent-deep': '#214731', '--accent-soft': '#CFDFCB',
      '--gold': '#9D936A',
      '--c-green': '#3F6B4B', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#080A07',
    },

    'navy-light': {
      '--paper': '#F2EEE5', '--paper-deep': '#E5E1D5', '--paper-edge': '#CFC9B9',
      '--paper-card': '#FAF7F0', '--paper-on-photo': '#F3EFE5',
      '--ink': '#11161F', '--ink-soft': '#2A2F3B', '--ink-muted': '#6F7886', '--ink-faint': '#A2A8B5',
      '--rule': '#C2C7CF', '--rule-soft': '#DCDFE3',
      '--accent': '#2A4A7A', '--accent-deep': '#152846', '--accent-soft': '#D0DCEC',
      '--gold': '#7E8AA0',
      '--c-green': '#5A786A', '--c-teal': '#2D5C66', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#060810',
    },

    // ── New light themes ──────────────────────────────────────────────────────

    // Linen — crisp pale linen, deep navy accent.
    linen: {
      '--paper': '#F7F4ED', '--paper-deep': '#ECE7DA', '--paper-edge': '#D6CFBE',
      '--paper-card': '#FCFAF3', '--paper-on-photo': '#F4F0E5',
      '--ink': '#13171F', '--ink-soft': '#2E333D', '--ink-muted': '#717686', '--ink-faint': '#A8ACB8',
      '--rule': '#CFCABC', '--rule-soft': '#E3DFD2',
      '--accent': '#1F3A66', '--accent-deep': '#0F1F40', '--accent-soft': '#D8DFEC',
      '--gold': '#A4944F',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#06080F',
    },

    // Rose Paper — pale rose paper, wine red accent.
    'rose-paper': {
      '--paper': '#F6EDE8', '--paper-deep': '#EEE0D8', '--paper-edge': '#DECABD',
      '--paper-card': '#FBF4EF', '--paper-on-photo': '#F8EFEA',
      '--ink': '#1E1310', '--ink-soft': '#3F2E2A', '--ink-muted': '#84675D', '--ink-faint': '#B8A097',
      '--rule': '#D5BFB3', '--rule-soft': '#E8D6CC',
      '--accent': '#8A2A3F', '--accent-deep': '#5A1224', '--accent-soft': '#EFD4D6',
      '--gold': '#B68F58',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0E0606',
    },

    // Sage — pale sage paper, deep moss accent.
    sage: {
      '--paper': '#EEEEE2', '--paper-deep': '#E0E2D0', '--paper-edge': '#CBCEB6',
      '--paper-card': '#F6F6EB', '--paper-on-photo': '#F0F1E4',
      '--ink': '#161A14', '--ink-soft': '#2E3328', '--ink-muted': '#6F7866', '--ink-faint': '#A6AC9C',
      '--rule': '#BEC4B0', '--rule-soft': '#D4D8C4',
      '--accent': '#34563A', '--accent-deep': '#1B341F', '--accent-soft': '#CFD9C6',
      '--gold': '#9E9656',
      '--c-green': '#34563A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#080A06',
    },

    // ── More light themes ─────────────────────────────────────────────────────

    // Olive & Gold — warm olive-green accent with a gold companion.
    'olive-gold': {
      '--paper': '#F3F0E2', '--paper-deep': '#E7E3CF', '--paper-edge': '#D2CDB2',
      '--paper-card': '#F9F6EA', '--paper-on-photo': '#F4F1E3',
      '--ink': '#181A12', '--ink-soft': '#33362A', '--ink-muted': '#767A62', '--ink-faint': '#ABAD92',
      '--rule': '#C6C4A6', '--rule-soft': '#DDDAC0',
      '--accent': '#6B7A33', '--accent-deep': '#48521E', '--accent-soft': '#E1E3C4',
      '--gold': '#B6923F',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0A0B05',
    },

    // Sea Glass — pale aqua-green paper, deep teal-green accent.
    'sea-glass': {
      '--paper': '#EBF1EC', '--paper-deep': '#DBE6DD', '--paper-edge': '#C3D3C7',
      '--paper-card': '#F3F8F3', '--paper-on-photo': '#ECF2ED',
      '--ink': '#101815', '--ink-soft': '#2A342F', '--ink-muted': '#647069', '--ink-faint': '#9EAAA2',
      '--rule': '#BCCBC1', '--rule-soft': '#D2DED6',
      '--accent': '#2E7D6E', '--accent-deep': '#155448', '--accent-soft': '#CBE4DC',
      '--gold': '#9FA36A',
      '--c-green': '#3E8A72', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#05100C',
    },

    // Fern & Copper — pale green paper, fern-green accent, copper gold.
    'fern-copper': {
      '--paper': '#EFEEE1', '--paper-deep': '#E1E1CF', '--paper-edge': '#CBCCB4',
      '--paper-card': '#F6F5E9', '--paper-on-photo': '#F0EFE2',
      '--ink': '#15180F', '--ink-soft': '#2F3327', '--ink-muted': '#6F7660', '--ink-faint': '#A6AB90',
      '--rule': '#C1C4A8', '--rule-soft': '#D8DAC2',
      '--accent': '#4E7B45', '--accent-deep': '#2E5228', '--accent-soft': '#D6E2C9',
      '--gold': '#B57A45',
      '--c-green': '#4E7B45', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#080A05',
    },

    // Honey & Sage — warm paper, sage-green accent, honey gold.
    'honey-sage': {
      '--paper': '#F3F1E4', '--paper-deep': '#E7E4D1', '--paper-edge': '#D2CEB4',
      '--paper-card': '#F9F7EC', '--paper-on-photo': '#F4F2E5',
      '--ink': '#171810', '--ink-soft': '#323428', '--ink-muted': '#767962', '--ink-faint': '#ABAD92',
      '--rule': '#C7C5A8', '--rule-soft': '#DEDBC2',
      '--accent': '#5E7A50', '--accent-deep': '#3C5230', '--accent-soft': '#DCE4CC',
      '--gold': '#C29A44',
      '--c-green': '#5E7A50', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0A0B04',
    },

    // Lavender Haze — light purple paper, violet accent.
    'lavender-haze': {
      '--paper': '#F1EEF6', '--paper-deep': '#E4DEEE', '--paper-edge': '#D0C6E0',
      '--paper-card': '#F7F4FB', '--paper-on-photo': '#F2EEF8',
      '--ink': '#16121E', '--ink-soft': '#322C3D', '--ink-muted': '#736A85', '--ink-faint': '#A9A0BC',
      '--rule': '#CBC2DC', '--rule-soft': '#DED7EA',
      '--accent': '#6E4CB0', '--accent-deep': '#472E80', '--accent-soft': '#E0D6F2',
      '--gold': '#9E8A86',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#8A6EA8',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0A0710',
    },

    // Lilac & Sage — light purple paper, muted violet accent, green companion.
    'lilac-sage': {
      '--paper': '#EFEFEC', '--paper-deep': '#E2E2DB', '--paper-edge': '#CCCEC2',
      '--paper-card': '#F6F6F1', '--paper-on-photo': '#F0F0EB',
      '--ink': '#171620', '--ink-soft': '#312F3A', '--ink-muted': '#71707E', '--ink-faint': '#A7A6B2',
      '--rule': '#C6C6BE', '--rule-soft': '#DAD9D0',
      '--accent': '#7A5C9E', '--accent-deep': '#513A70', '--accent-soft': '#E1D9EC',
      '--gold': '#9C9A66',
      '--c-green': '#5F7E60', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#8E72AA',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#09080E',
    },

    // Wisteria — pale lilac paper, blue-violet accent.
    wisteria: {
      '--paper': '#EEEFF6', '--paper-deep': '#E0E2EE', '--paper-edge': '#C8CCE0',
      '--paper-card': '#F5F6FB', '--paper-on-photo': '#F0F1F8',
      '--ink': '#12141F', '--ink-soft': '#2C2F3C', '--ink-muted': '#6C7286', '--ink-faint': '#A2A8BC',
      '--rule': '#C4C8DC', '--rule-soft': '#D8DBEA',
      '--accent': '#5A5EC0', '--accent-deep': '#383C90', '--accent-soft': '#D6D9F2',
      '--gold': '#8E92B4',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#8072B4',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#07080F',
    },

    // Amethyst & Cream — cream paper, amethyst-purple accent, gold companion.
    'amethyst-cream': {
      '--paper': '#F5F1EC', '--paper-deep': '#EAE3DA', '--paper-edge': '#D8CEC0',
      '--paper-card': '#FBF7F1', '--paper-on-photo': '#F5F1EB',
      '--ink': '#1A141C', '--ink-soft': '#372E38', '--ink-muted': '#7E7080', '--ink-faint': '#B4A6B4',
      '--rule': '#D2C6CC', '--rule-soft': '#E4DADE',
      '--accent': '#8A3F86', '--accent-deep': '#5C2258', '--accent-soft': '#EEDAEA',
      '--gold': '#B6923F',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9A5E96',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0C060B',
    },

    // Blush — soft rose paper, dusty-rose accent.
    blush: {
      '--paper': '#F6EEEC', '--paper-deep': '#EBDEDA', '--paper-edge': '#DAC6C0',
      '--paper-card': '#FCF4F1', '--paper-on-photo': '#F6EEEB',
      '--ink': '#1D1412', '--ink-soft': '#3D2F2C', '--ink-muted': '#84685F', '--ink-faint': '#B8A099',
      '--rule': '#D6BFB8', '--rule-soft': '#E8D6D0',
      '--accent': '#B0596A', '--accent-deep': '#7E3040', '--accent-soft': '#F1DBDD',
      '--gold': '#BA9058',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#A5788C',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#0E0706',
    },

    // Harbor — pale blue-gray paper, slate-teal accent.
    harbor: {
      '--paper': '#ECEFEF', '--paper-deep': '#DCE2E2', '--paper-edge': '#C3CDCD',
      '--paper-card': '#F4F7F7', '--paper-on-photo': '#EDF0F0',
      '--ink': '#101619', '--ink-soft': '#2A3235', '--ink-muted': '#647074', '--ink-faint': '#9EAAAD',
      '--rule': '#BECACB', '--rule-soft': '#D4DEDE',
      '--accent': '#2E6C82', '--accent-deep': '#164657', '--accent-soft': '#CDE0E7',
      '--gold': '#9AA49E',
      '--c-green': '#5A786A', '--c-teal': '#38788E', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4F6B47', '--err': '#A53623', '--shadow': '#050C0F',
    },

    // ── Dark themes ───────────────────────────────────────────────────────────
    // All dark themes use dark paper surfaces so the entire UI goes dark.
    // paper-on-photo stays light (cream/white) so text on photo heroes stays readable.

    // 1. Midnight — deep navy, steel-blue accent
    midnight: {
      '--paper': '#090E1A', '--paper-deep': '#101626', '--paper-edge': '#1C2638',
      '--paper-card': '#0D1422', '--paper-on-photo': '#E0E8F2',
      '--ink': '#DCE5F0', '--ink-soft': '#A8B4C8', '--ink-muted': '#5A6A80', '--ink-faint': '#2C384C',
      '--rule': '#1C2A3C', '--rule-soft': '#131E2C',
      '--accent': '#5880C8', '--accent-deep': '#3460B0', '--accent-soft': '#18264A',
      '--gold': '#90A8CC',
      '--c-green': '#487870', '--c-teal': '#3878A0', '--c-slate': '#3A5068', '--c-mauve': '#8070A0',
      '--ok': '#48B07A', '--err': '#D85050', '--shadow': '#02040A',
    },

    // 2. Obsidian — near-black warm, amber-gold accent
    obsidian: {
      '--paper': '#0C0A08', '--paper-deep': '#161210', '--paper-edge': '#242018',
      '--paper-card': '#181410', '--paper-on-photo': '#F2ECC8',
      '--ink': '#EEE8C8', '--ink-soft': '#C4B090', '--ink-muted': '#7A6850', '--ink-faint': '#3C3020',
      '--rule': '#2E2418', '--rule-soft': '#201810',
      '--accent': '#C89840', '--accent-deep': '#8C6820', '--accent-soft': '#281E08',
      '--gold': '#E0B858',
      '--c-green': '#5C6E58', '--c-teal': '#486868', '--c-slate': '#3E5060', '--c-mauve': '#7E6070',
      '--ok': '#48A060', '--err': '#C84038', '--shadow': '#06040A',
    },

    // 3. Espresso — dark roasted brown, amber accent
    espresso: {
      '--paper': '#0E0A07', '--paper-deep': '#1A1410', '--paper-edge': '#2A2018',
      '--paper-card': '#180E0A', '--paper-on-photo': '#F4E8C8',
      '--ink': '#EEE0C0', '--ink-soft': '#C8A888', '--ink-muted': '#8A6840', '--ink-faint': '#402C18',
      '--rule': '#301E14', '--rule-soft': '#20140C',
      '--accent': '#D09840', '--accent-deep': '#906818', '--accent-soft': '#281808',
      '--gold': '#E8B850',
      '--c-green': '#4E6040', '--c-teal': '#3A6050', '--c-slate': '#3A5048', '--c-mauve': '#7A4862',
      '--ok': '#48A050', '--err': '#C84030', '--shadow': '#0A0604',
    },

    // 4. Plum — dark purple/plum, violet accent
    plum: {
      '--paper': '#0E0818', '--paper-deep': '#160E28', '--paper-edge': '#26183E',
      '--paper-card': '#120C20', '--paper-on-photo': '#EEE8F8',
      '--ink': '#EAE0F8', '--ink-soft': '#C0B0D8', '--ink-muted': '#7868A0', '--ink-faint': '#3C2A50',
      '--rule': '#261640', '--rule-soft': '#180E2C',
      '--accent': '#9E68E0', '--accent-deep': '#6A38B8', '--accent-soft': '#1E1040',
      '--gold': '#D09858',
      '--c-green': '#4A6C5E', '--c-teal': '#3E6082', '--c-slate': '#384858', '--c-mauve': '#9E72B2',
      '--ok': '#48B870', '--err': '#E04860', '--shadow': '#06020E',
    },

    // 5. Dusk Navy — cool dark slate-navy, teal accent
    'dusk-navy': {
      '--paper': '#0C1018', '--paper-deep': '#121820', '--paper-edge': '#1E2830',
      '--paper-card': '#101620', '--paper-on-photo': '#DDEAF2',
      '--ink': '#D8E6F0', '--ink-soft': '#A8B8C8', '--ink-muted': '#587080', '--ink-faint': '#283040',
      '--rule': '#1C2830', '--rule-soft': '#141E28',
      '--accent': '#38A090', '--accent-deep': '#207878', '--accent-soft': '#0C2028',
      '--gold': '#78B8A0',
      '--c-green': '#488068', '--c-teal': '#388898', '--c-slate': '#3C5870', '--c-mauve': '#7C70A0',
      '--ok': '#48B880', '--err': '#D05048', '--shadow': '#04080E',
    },

    // 6. Charcoal — warm dark gray, rust/terracotta accent
    charcoal: {
      '--paper': '#111010', '--paper-deep': '#1A1818', '--paper-edge': '#282420',
      '--paper-card': '#181414', '--paper-on-photo': '#F2ECEC',
      '--ink': '#EEE8E8', '--ink-soft': '#C4B8B0', '--ink-muted': '#7A6C64', '--ink-faint': '#3C3028',
      '--rule': '#28201A', '--rule-soft': '#1C1810',
      '--accent': '#B06040', '--accent-deep': '#7A3818', '--accent-soft': '#281008',
      '--gold': '#D09060',
      '--c-green': '#587050', '--c-teal': '#406860', '--c-slate': '#485062', '--c-mauve': '#907082',
      '--ok': '#50A850', '--err': '#C84838', '--shadow': '#0A0808',
    },

    // ── New dark themes ───────────────────────────────────────────────────────

    // Moss — dark green-black, warm gold accent.
    moss: {
      '--paper': '#0A1108', '--paper-deep': '#121A0E', '--paper-edge': '#1E2C18',
      '--paper-card': '#10180C', '--paper-on-photo': '#E8F0DC',
      '--ink': '#E4EED4', '--ink-soft': '#B4BFA0', '--ink-muted': '#6A7A5C', '--ink-faint': '#2E3A24',
      '--rule': '#1E2C18', '--rule-soft': '#162010',
      '--accent': '#C8A050', '--accent-deep': '#8C6820', '--accent-soft': '#1A2410',
      '--gold': '#E0C070',
      '--c-green': '#6A8870', '--c-teal': '#487868', '--c-slate': '#4A5C58', '--c-mauve': '#90856E',
      '--ok': '#5BA670', '--err': '#D85048', '--shadow': '#040A02',
    },

    // Wine — deep aubergine/wine, pale gold accent.
    wine: {
      '--paper': '#160810', '--paper-deep': '#1F0E18', '--paper-edge': '#341A28',
      '--paper-card': '#1A0C14', '--paper-on-photo': '#F2E4DE',
      '--ink': '#EEDFD8', '--ink-soft': '#C0A89E', '--ink-muted': '#806068', '--ink-faint': '#40242C',
      '--rule': '#34182A', '--rule-soft': '#22101A',
      '--accent': '#D49770', '--accent-deep': '#A06A45', '--accent-soft': '#28121C',
      '--gold': '#E4B080',
      '--c-green': '#5E7A6A', '--c-teal': '#4A6E80', '--c-slate': '#48505E', '--c-mauve': '#A07088',
      '--ok': '#5BA670', '--err': '#E07060', '--shadow': '#0C0408',
    },

    // Cobalt Noir — deep ink-blue, electric cobalt accent.
    'cobalt-noir': {
      '--paper': '#06080F', '--paper-deep': '#0C1019', '--paper-edge': '#161C2A',
      '--paper-card': '#0A0E18', '--paper-on-photo': '#E2EAF2',
      '--ink': '#DCE4F0', '--ink-soft': '#A8B0C2', '--ink-muted': '#5C657A', '--ink-faint': '#283044',
      '--rule': '#16203A', '--rule-soft': '#0E1626',
      '--accent': '#4080F0', '--accent-deep': '#2058C0', '--accent-soft': '#0C1A38',
      '--gold': '#88A4D8',
      '--c-green': '#4A7868', '--c-teal': '#3878A0', '--c-slate': '#3A4E68', '--c-mauve': '#7868A0',
      '--ok': '#5BA670', '--err': '#E07060', '--shadow': '#02040A',
    },

    // ── More dark themes ──────────────────────────────────────────────────────

    // Emerald Noir — dark green-black, emerald accent, warm gold.
    'emerald-noir': {
      '--paper': '#07120C', '--paper-deep': '#0E1C14', '--paper-edge': '#183020',
      '--paper-card': '#0C1810', '--paper-on-photo': '#E2F0E4',
      '--ink': '#DEEEDE', '--ink-soft': '#AEC2B2', '--ink-muted': '#5E7862', '--ink-faint': '#283C2C',
      '--rule': '#183020', '--rule-soft': '#102416',
      '--accent': '#2FA672', '--accent-deep': '#1C7450', '--accent-soft': '#0E2418',
      '--gold': '#C8A860',
      '--c-green': '#4FA07A', '--c-teal': '#3E8890', '--c-slate': '#3A5C58', '--c-mauve': '#8A80A0',
      '--ok': '#4DB378', '--err': '#E05858', '--shadow': '#020A05',
    },

    // Pine & Gold — dark pine green, gold accent.
    'pine-gold': {
      '--paper': '#0A1310', '--paper-deep': '#111E18', '--paper-edge': '#1D3028',
      '--paper-card': '#0E1A15', '--paper-on-photo': '#E8F0E8',
      '--ink': '#E2EEE4', '--ink-soft': '#B0C2B4', '--ink-muted': '#607A68', '--ink-faint': '#2A3C30',
      '--rule': '#1D3028', '--rule-soft': '#14231C',
      '--accent': '#C8A050', '--accent-deep': '#8E6C22', '--accent-soft': '#18241A',
      '--gold': '#E4C674',
      '--c-green': '#5A9078', '--c-teal': '#448082', '--c-slate': '#3E5C56', '--c-mauve': '#8C8272',
      '--ok': '#4DB378', '--err': '#E05858', '--shadow': '#030A06',
    },

    // Deep Jade — dark teal-green, bright jade accent.
    'deep-jade': {
      '--paper': '#06120F', '--paper-deep': '#0C1C18', '--paper-edge': '#163029',
      '--paper-card': '#0A1814', '--paper-on-photo': '#DEF0EC',
      '--ink': '#D8EEE8', '--ink-soft': '#A6C2BC', '--ink-muted': '#587872', '--ink-faint': '#243C36',
      '--rule': '#163029', '--rule-soft': '#0E241E',
      '--accent': '#26A896', '--accent-deep': '#147468', '--accent-soft': '#0A241E',
      '--gold': '#84BCA8',
      '--c-green': '#409886', '--c-teal': '#2E9498', '--c-slate': '#3A5E5C', '--c-mauve': '#7E86A0',
      '--ok': '#4DB378', '--err': '#E05858', '--shadow': '#020A08',
    },

    // Teal Abyss — dark teal, electric teal accent.
    'teal-abyss': {
      '--paper': '#06110F', '--paper-deep': '#0B1C1A', '--paper-edge': '#143030',
      '--paper-card': '#081816', '--paper-on-photo': '#DCF0EE',
      '--ink': '#D6EEEC', '--ink-soft': '#A4C2C0', '--ink-muted': '#547876', '--ink-faint': '#223C3A',
      '--rule': '#143030', '--rule-soft': '#0C2422',
      '--accent': '#28B0B0', '--accent-deep': '#147C7C', '--accent-soft': '#0A2424',
      '--gold': '#7CC0BE',
      '--c-green': '#409888', '--c-teal': '#2CA0A8', '--c-slate': '#385E60', '--c-mauve': '#7C86A0',
      '--ok': '#4DB378', '--err': '#E05858', '--shadow': '#020A08',
    },

    // Violet Dusk — dark purple, light violet accent.
    'violet-dusk': {
      '--paper': '#0D0918', '--paper-deep': '#150F28', '--paper-edge': '#241A40',
      '--paper-card': '#110C20', '--paper-on-photo': '#ECE6F8',
      '--ink': '#E8E0F6', '--ink-soft': '#BDB0D6', '--ink-muted': '#76689E', '--ink-faint': '#382850',
      '--rule': '#241A40', '--rule-soft': '#170F2A',
      '--accent': '#9C74E0', '--accent-deep': '#6A44B4', '--accent-soft': '#1C1038',
      '--gold': '#C6A0D2',
      '--c-green': '#5A7E70', '--c-teal': '#4A6C90', '--c-slate': '#3E4A60', '--c-mauve': '#9E78BC',
      '--ok': '#4DB378', '--err': '#E05868', '--shadow': '#05020E',
    },

    // Orchid Noir — dark plum, orchid (light purple) accent.
    'orchid-noir': {
      '--paper': '#120A16', '--paper-deep': '#1C1024', '--paper-edge': '#301C3A',
      '--paper-card': '#160C1C', '--paper-on-photo': '#F0E6F2',
      '--ink': '#EEE0F0', '--ink-soft': '#C6AECC', '--ink-muted': '#846A88', '--ink-faint': '#3E2C42',
      '--rule': '#301C3A', '--rule-soft': '#1F1226',
      '--accent': '#C06AC8', '--accent-deep': '#8A3E92', '--accent-soft': '#24122A',
      '--gold': '#CDA0A8',
      '--c-green': '#5A7A6A', '--c-teal': '#4A6A88', '--c-slate': '#484A62', '--c-mauve': '#B074B4',
      '--ok': '#4DB378', '--err': '#E05868', '--shadow': '#08040A',
    },

    // Amethyst Dark — dark purple-gray, amethyst accent, gold companion.
    'amethyst-dark': {
      '--paper': '#0F0C14', '--paper-deep': '#181320', '--paper-edge': '#282034',
      '--paper-card': '#130F1A', '--paper-on-photo': '#ECE8F4',
      '--ink': '#E6E2F0', '--ink-soft': '#B8B0C8', '--ink-muted': '#726A84', '--ink-faint': '#342C40',
      '--rule': '#282034', '--rule-soft': '#1A1526',
      '--accent': '#A070D8', '--accent-deep': '#6E44A8', '--accent-soft': '#1C1430',
      '--gold': '#D6B46A',
      '--c-green': '#5A7868', '--c-teal': '#48688C', '--c-slate': '#3E485C', '--c-mauve': '#9A78B8',
      '--ok': '#4DB378', '--err': '#E05860', '--shadow': '#050309',
    },

    // Indigo Night — dark indigo, periwinkle accent.
    'indigo-night': {
      '--paper': '#080A16', '--paper-deep': '#0E1224', '--paper-edge': '#1A2038',
      '--paper-card': '#0B0E1E', '--paper-on-photo': '#E4E8F6',
      '--ink': '#DEE2F2', '--ink-soft': '#AEB4CE', '--ink-muted': '#606890', '--ink-faint': '#2A3048',
      '--rule': '#1A2038', '--rule-soft': '#111628',
      '--accent': '#7A88E8', '--accent-deep': '#4E5CC0', '--accent-soft': '#141838',
      '--gold': '#A0A8D8',
      '--c-green': '#4E7C74', '--c-teal': '#4074A0', '--c-slate': '#3C4A6A', '--c-mauve': '#8478B0',
      '--ok': '#4DB378', '--err': '#E05868', '--shadow': '#02030C',
    },

    // Rosewood — dark rose-brown, dusty-pink accent.
    rosewood: {
      '--paper': '#130A0C', '--paper-deep': '#1E1014', '--paper-edge': '#341E24',
      '--paper-card': '#180C10', '--paper-on-photo': '#F2E2E2',
      '--ink': '#EEDCDC', '--ink-soft': '#C6A6A6', '--ink-muted': '#846266', '--ink-faint': '#3E262A',
      '--rule': '#341E24', '--rule-soft': '#221318',
      '--accent': '#C87888', '--accent-deep': '#94485A', '--accent-soft': '#28141A',
      '--gold': '#D0A084',
      '--c-green': '#5E7A6A', '--c-teal': '#4A6E80', '--c-slate': '#4A505E', '--c-mauve': '#A87890',
      '--ok': '#4DB378', '--err': '#E06860', '--shadow': '#0A0406',
    },

    // ── Classic dark (kept for reference) ────────────────────────────────────

    'dark-frantzen': {
      '--paper': '#0E1014', '--paper-deep': '#14171D', '--paper-edge': '#1F232B',
      '--paper-card': '#181B22', '--paper-on-photo': '#ECEEF2',
      '--ink': '#F0F2F6', '--ink-soft': '#C4C8D2', '--ink-muted': '#7A8090', '--ink-faint': '#4A4E58',
      '--rule': '#2A2E36', '--rule-soft': '#1E2128',
      '--accent': '#5878E8', '--accent-deep': '#3854D0', '--accent-soft': '#1A2440',
      '--gold': '#8AA4FF',
      '--c-green': '#5A786A', '--c-teal': '#3E6D77', '--c-slate': '#3E5359', '--c-mauve': '#9D8499',
      '--ok': '#4DB378', '--err': '#E54F4F', '--shadow': '#04060A',
    },

    'dark-white': {
      '--paper': '#0A0A0A', '--paper-deep': '#121214', '--paper-edge': '#26262A',
      '--paper-card': '#17171A', '--paper-on-photo': '#F5F5F5',
      '--ink': '#FFFFFF', '--ink-soft': '#D8D8D8', '--ink-muted': '#8C8C8C', '--ink-faint': '#454548',
      '--rule': '#2A2A2D', '--rule-soft': '#1A1A1D',
      '--accent': '#FFFFFF', '--accent-deep': '#C8C8C8', '--accent-soft': '#2A2A2D',
      '--gold': '#BCBCBC',
      '--c-green': '#7B9685', '--c-teal': '#558693', '--c-slate': '#5B6B71', '--c-mauve': '#A697A1',
      '--ok': '#5BC287', '--err': '#EF5A5A', '--shadow': '#000000',
    },
  };

  // ────────────────────────────────────────────────────────────────────────────
  // STATE & STORAGE
  //
  // Source of truth is the server (KV via /api/palette). localStorage is a
  // FAST CACHE so the editor doesn't blank-flash while the network round-trip
  // happens — we hydrate from cache instantly, then reconcile against the
  // server response a moment later.
  // ────────────────────────────────────────────────────────────────────────────
  const KEY        = 'zahara-color-overrides';
  // Which venue this editor edits, from the admin site-switch cookie (set by the
  // venue switch in the topbar / other /admin pages). The seed fetch is scoped
  // to it so we never seed from Zahara and then save into rooftop; the save
  // endpoint (/admin/colors/save) reads the same cookie server-side.
  const ADMIN_SITE = (document.cookie.match(/(?:^|;\s*)zahara_admin_site=(rooftop|zahara)/) || [])[1] || 'zahara';
  const SITE_Q     = ADMIN_SITE === 'rooftop' ? '?site=rooftop' : '';
  const SERVER_GET = '/api/palette' + SITE_Q;
  const SERVER_PUT = '/admin/colors/save';
  const root       = document.documentElement;
  const rows       = Array.from(document.querySelectorAll('[data-row]'));

  // Which palette we're editing: 'light' or 'dark'. The preview pane reflects
  // this mode; Save persists BOTH. State shape everywhere is a pair:
  //   { light: { '--token': '#hex', … }, dark: { … } }
  let activeMode = 'light';

  let bc = null;
  try { bc = new BroadcastChannel('zahara-colors'); } catch (_) {}

  // ── Pair helpers ────────────────────────────────────────────────────────────
  function emptyPair() { return { light: {}, dark: {} }; }
  function clonePair(p) { return { light: Object.assign({}, p.light), dark: Object.assign({}, p.dark) }; }

  // Normalise any stored / fetched value into the pair shape, migrating an
  // old flat { '--token':'#hex' } record (light only).
  function toPair(raw) {
    const pair = emptyPair();
    if (!raw || typeof raw !== 'object') return pair;
    const looksFlat = Object.keys(raw).some((k) => k.indexOf('--') === 0);
    const src = looksFlat ? { light: raw } : raw;
    ['light', 'dark'].forEach((mode) => {
      const m = src[mode];
      if (m && typeof m === 'object') {
        for (const t in m) {
          if (t.indexOf('--') === 0 && typeof m[t] === 'string') pair[mode][t] = String(m[t]).toUpperCase();
        }
      }
    });
    return pair;
  }

  function defaultFor(mode, token) {
    return ((mode === 'dark' ? darkDefaults[token] : defaults[token]) || defaults[token] || '').toUpperCase();
  }
  function rowDefault(row) {
    return (activeMode === 'dark' ? row.dataset.defaultDark : row.dataset.default).toUpperCase();
  }
  function curMap() { return currentState[activeMode]; }
  function resolved(mode, token) {
    return (currentState[mode][token] || defaultFor(mode, token)).toUpperCase();
  }

  function loadCache() {
    try { return toPair(JSON.parse(localStorage.getItem(KEY) || '{}')); } catch { return emptyPair(); }
  }
  function writeCache(o) {
    try { localStorage.setItem(KEY, JSON.stringify(o)); } catch (_) {}
  }

  // savedState: last value confirmed against the SERVER.
  // currentState: live state including unsaved edits.
  // Start optimistically from the cache; reconcile with the server below.
  let savedState   = loadCache();
  let currentState = clonePair(savedState);
  let serverSyncedAt = 0;  // becomes >0 once /api/palette has returned

  // ────────────────────────────────────────────────────────────────────────────
  // UI REFS
  // ────────────────────────────────────────────────────────────────────────────
  const toastEl      = document.querySelector('[data-toast]');
  const undoBtn      = document.querySelector('[data-undo]');
  const saveBtn      = document.querySelector('[data-save]');
  const discardBtn   = document.querySelector('[data-discard]');
  const pendingBadge = document.querySelector('[data-pending-badge]');

  // ── Render the active mode onto :root + the rows ──────────────────────────────
  // We ALWAYS set an explicit value for every token (never removeProperty):
  // the admin page carries no data-theme attribute, so relying on the cascade
  // would fall back to the LIGHT default even while editing dark.
  function renderMode() {
    rows.forEach((row) => {
      const val = resolved(activeMode, row.dataset.token);
      root.style.setProperty(row.dataset.token, val);
      _syncRowUI(row, val, { skipHistory: true });
    });
    _refreshAllContrast();
    _updatePendingUI();
  }

  // ────────────────────────────────────────────────────────────────────────────
  // INIT — render cached state immediately (zero flash), then reconcile
  // against the canonical server state.
  // ────────────────────────────────────────────────────────────────────────────
  renderMode();

  // Fire-and-forget reconciliation. We deliberately don't `await` this at
  // the top level — the editor renders from cache immediately, and the
  // server response slides values into place a moment later.
  (async function reconcileWithServer() {
    let serverPair;
    try {
      const res = await fetch(SERVER_GET, { cache: 'no-store', credentials: 'same-origin' });
      if (!res.ok) throw new Error('HTTP ' + res.status);
      serverPair = toPair(await res.json());
    } catch (err) {
      console.warn('[colors] /api/palette failed — staying on cached state', err);
      return;
    }
    serverSyncedAt = Date.now();

    // Server is canonical for savedState. Pending edits stay pending.
    savedState = serverPair;
    writeCache(savedState);

    // If the user hasn't started editing yet, bring currentState in line with
    // the server so the picker UI matches what's actually saved.
    if (history.length === 0) {
      currentState = clonePair(savedState);
      renderMode();
    } else {
      _refreshAllDots();
      _updatePendingUI();
    }
  })();

  // ────────────────────────────────────────────────────────────────────────────
  // UNDO HISTORY
  // ────────────────────────────────────────────────────────────────────────────
  const history = [];

  function _pushHistory(token, from, to) {
    history.push({ mode: activeMode, token, from, to });
    if (history.length > 50) history.shift();
    undoBtn.disabled = false;
  }

  function undo() {
    const last = history.pop();
    if (!last) { undoBtn.disabled = true; return; }
    if (last.mode !== activeMode) setMode(last.mode);
    applyToken(last.token, last.from, { silent: true });
    if (history.length === 0) undoBtn.disabled = true;
  }

  undoBtn.addEventListener('click', undo);

  // ────────────────────────────────────────────────────────────────────────────
  // PENDING TRACKING
  // ────────────────────────────────────────────────────────────────────────────
  function isPendingIn(mode, token) {
    const cur = (currentState[mode][token] || defaultFor(mode, token)).toUpperCase();
    const sav = (savedState[mode][token]   || defaultFor(mode, token)).toUpperCase();
    return cur !== sav;
  }
  function _isPending(token) { return isPendingIn(activeMode, token); }

  // Counts rows that differ from saved in EITHER mode, so the Save button
  // and "N unsaved" badge reflect edits made across both palettes.
  function _countPending() {
    return rows.filter((r) =>
      isPendingIn('light', r.dataset.token) || isPendingIn('dark', r.dataset.token)
    ).length;
  }

  function _updatePendingUI() {
    const n = _countPending();
    if (n > 0) {
      pendingBadge.textContent = n + ' unsaved';
      pendingBadge.hidden = false;
    } else {
      pendingBadge.hidden = true;
    }
    saveBtn.disabled    = n === 0;
    discardBtn.disabled = n === 0;
  }

  // ────────────────────────────────────────────────────────────────────────────
  // SAVE — POST currentState to the server. Server is canonical; only on a
  // 2xx do we update savedState (so a network failure leaves the editor
  // still showing "N unsaved" and the user can retry).
  // ────────────────────────────────────────────────────────────────────────────
  let saving = false;

  async function save() {
    if (saving) return;
    if (_countPending() === 0) return;
    saving = true;
    const label = saveBtn.textContent;
    saveBtn.disabled = true;
    saveBtn.textContent = 'Saving…';

    try {
      const res = await fetch(SERVER_PUT, {
        method:      'POST',
        headers:     { 'Content-Type': 'application/json' },
        credentials: 'same-origin',
        body:        JSON.stringify(currentState),
      });
      if (!res.ok) {
        let msg = 'HTTP ' + res.status;
        try { msg = (await res.json()).error || msg; } catch (_) {}
        throw new Error(msg);
      }
      savedState = clonePair(currentState);
      writeCache(savedState);
      if (bc) bc.postMessage({ type: 'colors-changed', overrides: savedState });
      showToast('Saved to server ✓');
    } catch (err) {
      console.error('[colors] save failed', err);
      showToast('Save failed: ' + err.message);
    } finally {
      saving = false;
      saveBtn.textContent = label;
      _updatePendingUI();
      _refreshAllDots();
    }
  }

  saveBtn.addEventListener('click', save);

  // ────────────────────────────────────────────────────────────────────────────
  // DISCARD
  // ────────────────────────────────────────────────────────────────────────────
  function discard() {
    // Revert both palettes to the last saved state and repaint the active mode.
    currentState = clonePair(savedState);
    renderMode();
    history.length = 0;
    undoBtn.disabled = true;
    showToast('Changes discarded');
  }

  discardBtn.addEventListener('click', () => {
    if (_countPending() === 0 || !confirm('Discard all unsaved changes?')) return;
    discard();
  });

  // ────────────────────────────────────────────────────────────────────────────
  // APPLY TOKEN — applies to :root immediately (preview updates), tracks
  // the change as "pending" but does NOT write to localStorage.
  // ────────────────────────────────────────────────────────────────────────────
  function applyToken(token, value, opts) {
    opts = opts || {};
    const row = rows.find((r) => r.dataset.token === token);
    if (!row) return;
    const normalised = _normaliseHex(value);
    if (!normalised) return;

    const modeDefault = rowDefault(row);
    const prev = (curMap()[token] || modeDefault).toUpperCase();
    if (!opts.silent && prev !== normalised) {
      _pushHistory(token, prev, normalised);
    }

    // Track override-vs-default in state, but ALWAYS paint :root explicitly
    // (the admin page has no data-theme, so removeProperty would fall back to
    // the light default even in dark mode).
    if (normalised === modeDefault) delete curMap()[token];
    else                            curMap()[token] = normalised;
    root.style.setProperty(token, normalised);

    _syncRowUI(row, normalised, opts);
    _refreshAllContrast();
    if (!opts.silent) _highlight(token);
    _updatePendingUI();
  }

  // ────────────────────────────────────────────────────────────────────────────
  // SYNC ROW UI (picker + hex + dots)
  // ────────────────────────────────────────────────────────────────────────────
  function _syncRowUI(row, value, opts) {
    opts = opts || {};
    const picker = row.querySelector('[data-picker]');
    const hexEl  = row.querySelector('[data-hex]');
    const dot    = row.querySelector('[data-modified-dot]');

    if (picker.value.toUpperCase() !== value) picker.value = value;
    if (!opts.skipHexSync && document.activeElement !== hexEl) {
      hexEl.value = value;
    }
    hexEl.classList.remove('is-invalid');

    // Dot: orange = modified from the active mode's default; blue = unsaved.
    const isModified = value !== rowDefault(row);
    const isPend     = _isPending(row.dataset.token);
    dot.classList.toggle('is-modified', isModified && !isPend);
    dot.classList.toggle('is-pending',  isPend);
    dot.title = isPend ? 'Unsaved change' : (isModified ? 'Modified from default' : '');
  }

  function _refreshAllDots() {
    rows.forEach((row) => {
      const val = resolved(activeMode, row.dataset.token);
      _syncRowUI(row, val, { skipHexSync: true, skipHistory: true });
    });
  }

  // ────────────────────────────────────────────────────────────────────────────
  // WIRE EACH ROW
  // ────────────────────────────────────────────────────────────────────────────
  rows.forEach((row) => {
    const token    = row.dataset.token;
    const picker   = row.querySelector('[data-picker]');
    const hexEl    = row.querySelector('[data-hex]');
    const showBtn  = row.querySelector('[data-show]');
    const copyBtn  = row.querySelector('[data-copy]');
    const resetBtn = row.querySelector('[data-reset-one]');

    picker.addEventListener('input', () => {
      applyToken(token, picker.value);
    });

    let hexTimer = null;
    hexEl.addEventListener('input', () => {
      const v = _normaliseHex(hexEl.value);
      if (!v) { hexEl.classList.add('is-invalid'); return; }
      hexEl.classList.remove('is-invalid');
      clearTimeout(hexTimer);
      hexTimer = setTimeout(() => applyToken(token, v), 160);
    });
    hexEl.addEventListener('blur', () => {
      const v = _normaliseHex(hexEl.value);
      if (v) hexEl.value = v;
      else   hexEl.value = resolved(activeMode, token);
      hexEl.classList.remove('is-invalid');
    });
    hexEl.addEventListener('focus', () => hexEl.select());

    showBtn.addEventListener('click', () => {
      _highlight(token);
      showBtn.classList.remove('is-pulsing');
      requestAnimationFrame(() => showBtn.classList.add('is-pulsing'));
      setTimeout(() => showBtn.classList.remove('is-pulsing'), 1300);
    });

    copyBtn.addEventListener('click', async () => {
      const val = resolved(activeMode, token);
      try { await navigator.clipboard.writeText(val); } catch (_) {}
      showToast('Copied ' + val);
    });

    resetBtn.addEventListener('click', () => {
      applyToken(token, rowDefault(row));
    });
  });

  // ────────────────────────────────────────────────────────────────────────────
  // MODE TOGGLE (light / dark)
  // ────────────────────────────────────────────────────────────────────────────
  function setMode(mode) {
    if (mode !== 'light' && mode !== 'dark') return;
    activeMode = mode;
    document.querySelectorAll('[data-mode-btn]').forEach((b) => {
      const on = b.dataset.modeBtn === mode;
      b.classList.toggle('is-active', on);
      b.setAttribute('aria-pressed', on ? 'true' : 'false');
    });
    renderMode();   // repaint :root + rows + contrast for the new mode
    _applyFilter(); // "modified only" is mode-sensitive
  }
  document.querySelectorAll('[data-mode-btn]').forEach((b) => {
    b.addEventListener('click', () => setMode(b.dataset.modeBtn));
  });

  // ────────────────────────────────────────────────────────────────────────────
  // RESET ALL — resets the ACTIVE mode's tokens to their defaults.
  // ────────────────────────────────────────────────────────────────────────────
  document.querySelector('[data-reset]').addEventListener('click', () => {
    if (!confirm('Reset every token in ' + activeMode + ' mode to its default? This marks them as unsaved.')) return;
    rows.forEach((r) => { delete curMap()[r.dataset.token]; });
    renderMode();
    history.length = 0;
    undoBtn.disabled = true;
    showToast('Reset ' + activeMode + ' to defaults (not yet saved)');
  });

  // ────────────────────────────────────────────────────────────────────────────
  // PRESET
  // ────────────────────────────────────────────────────────────────────────────
  document.querySelector('[data-preset]').addEventListener('change', function () {
    const name = this.value;
    if (!name || !PRESETS[name]) return;
    const palette = PRESETS[name];
    // The events band has no per-preset values; derive it from the preset's
    // own surfaces/ink/accent so it harmonises with whatever theme is loaded
    // (cream band on light themes, dark band on dark themes) instead of
    // lingering from a previously-loaded preset.
    const pick = (tk) => (palette[tk] != null ? palette[tk] : defaultFor(activeMode, tk));
    const derived = {
      '--events-band-from':    pick('--paper-deep'),
      '--events-band-to':      pick('--paper'),
      '--events-band-text':    pick('--ink-soft'),
      '--events-band-num':     pick('--accent'),
      '--events-band-divider': pick('--rule'),
    };
    // Apply to EVERY editable token: the preset's value where it provides one,
    // else a derived band value, else the active mode's base default — so a
    // preset always lands on a clean, complete palette.
    rows.forEach(function (row) {
      const tk  = row.dataset.token;
      const val = (palette[tk] != null) ? palette[tk]
                : (derived[tk] != null) ? derived[tk]
                : defaultFor(activeMode, tk);
      applyToken(tk, val);
    });
    showToast('Preview: ' + name + ' → ' + activeMode + ' (not saved)');
    this.value = '';
  });

  // ────────────────────────────────────────────────────────────────────────────
  // SEARCH / FILTER
  // ────────────────────────────────────────────────────────────────────────────
  const searchInput      = document.querySelector('[data-search]');
  const modifiedOnlyChk  = document.querySelector('[data-modified-only]');

  function _applyFilter() {
    const q    = searchInput.value.trim().toLowerCase();
    const mOnly = modifiedOnlyChk.checked;
    rows.forEach((r) => {
      const matchQ = !q || r.dataset.searchKey.includes(q);
      const matchM = !mOnly || (curMap()[r.dataset.token] !== undefined &&
                                curMap()[r.dataset.token].toUpperCase() !== rowDefault(r));
      r.classList.toggle('is-hidden', !(matchQ && matchM));
    });
    document.querySelectorAll('[data-group]').forEach((g) => {
      const visible = g.querySelectorAll('[data-row]:not(.is-hidden)').length;
      g.style.display = visible ? '' : 'none';
    });
  }

  searchInput.addEventListener('input', _applyFilter);
  modifiedOnlyChk.addEventListener('change', _applyFilter);

  // ────────────────────────────────────────────────────────────────────────────
  // GROUP COLLAPSE / EXPAND
  // ────────────────────────────────────────────────────────────────────────────
  document.querySelectorAll('[data-group-toggle]').forEach((toggle) => {
    toggle.addEventListener('click', () => {
      toggle.closest('[data-group]').classList.toggle('is-collapsed');
    });
  });

  // ────────────────────────────────────────────────────────────────────────────
  // PALETTE POPUP
  // ────────────────────────────────────────────────────────────────────────────
  const palettePopup = document.querySelector('[data-palette-popup]');
  const paletteGrid  = document.querySelector('[data-palette-grid]');
  let activePaletteRow = null;

  function _openPalettePopup(btn, targetRow) {
    activePaletteRow = targetRow;

    // Populate swatches with all current token values
    paletteGrid.innerHTML = '';
    rows.forEach((r) => {
      const tk  = r.dataset.token;
      const val = resolved(activeMode, tk);
      const label = r.querySelector('.row__name').firstChild.textContent.trim();
      const sw = document.createElement('button');
      sw.className = 'palette-swatch';
      sw.style.background = val;
      sw.title = label + '\n' + val;
      sw.setAttribute('data-swatch-value', val);
      sw.setAttribute('aria-label', label + ': ' + val);
      paletteGrid.appendChild(sw);
    });

    // Position near button
    const rect = btn.getBoundingClientRect();
    const top  = rect.bottom + window.scrollY + 4;
    const left = Math.min(rect.left + window.scrollX, window.innerWidth - 230);
    palettePopup.style.top  = top  + 'px';
    palettePopup.style.left = left + 'px';
    palettePopup.hidden = false;
  }

  rows.forEach((row) => {
    row.querySelector('[data-palette-btn]').addEventListener('click', function (e) {
      if (!palettePopup.hidden && activePaletteRow === row) {
        palettePopup.hidden = true; activePaletteRow = null; return;
      }
      _openPalettePopup(this, row);
      e.stopPropagation();
    });
  });

  paletteGrid.addEventListener('click', (e) => {
    const sw = e.target.closest('.palette-swatch');
    if (!sw || !activePaletteRow) return;
    applyToken(activePaletteRow.dataset.token, sw.dataset.swatchValue);
    palettePopup.hidden = true;
    activePaletteRow = null;
  });

  document.addEventListener('click', (e) => {
    if (!palettePopup.hidden && !palettePopup.contains(e.target)) {
      palettePopup.hidden = true; activePaletteRow = null;
    }
  });

  // ────────────────────────────────────────────────────────────────────────────
  // KEYBOARD SHORTCUTS
  // ────────────────────────────────────────────────────────────────────────────
  document.addEventListener('keydown', (e) => {
    const meta = e.metaKey || e.ctrlKey;
    if (meta && !e.shiftKey && e.key === 's') {
      e.preventDefault();
      if (_countPending() > 0) save();
    }
    if (meta && !e.shiftKey && (e.key === 'z' || e.key === 'Z')) {
      e.preventDefault();
      undo();
    }
    if (e.key === 'Escape') {
      palettePopup.hidden = true;
      activePaletteRow = null;
      if (!exportModal.hidden) exportModal.hidden = true;
    }
  });

  // ────────────────────────────────────────────────────────────────────────────
  // CONTRAST
  // ────────────────────────────────────────────────────────────────────────────
  function _relLum(hex) {
    const m = hex.replace('#','').match(/.{2}/g);
    if (!m) return 0;
    const [r, g, b] = m.map((s) => {
      let c = parseInt(s, 16) / 255;
      return c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
    });
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  }
  function _contrastRatio(a, b) {
    const la = _relLum(a), lb = _relLum(b);
    const [lo, hi] = la > lb ? [lb, la] : [la, lb];
    return (hi + 0.05) / (lo + 0.05);
  }

  function _refreshAllContrast() {
    const paper = resolved(activeMode, '--paper');
    rows.forEach((row) => {
      const token = row.dataset.token;
      const isText = /^--(ink|accent|gold|c-|ok|err)/.test(token);
      const badge  = row.querySelector('[data-contrast]');
      if (!isText) { badge.textContent = ''; badge.className = 'row__contrast'; return; }
      const val = resolved(activeMode, token);
      const c   = _contrastRatio(val, paper);
      let band, label;
      if      (c >= 7)   { band = 'aaa'; label = c.toFixed(1) + ':1 AAA'; }
      else if (c >= 4.5) { band = 'aa';  label = c.toFixed(1) + ':1 AA';  }
      else if (c >= 3)   { band = 'aa';  label = c.toFixed(1) + ':1 AA-L';}
      else               { band = 'lo';  label = c.toFixed(1) + ':1 lo';  }
      badge.textContent = label;
      badge.className   = 'row__contrast row__contrast--' + band;
    });
  }

  // ────────────────────────────────────────────────────────────────────────────
  // HIGHLIGHT PREVIEW FRAGMENTS
  // ────────────────────────────────────────────────────────────────────────────
  let highlightTimer = null;

  function _highlight(token) {
    const short = token.replace(/^--/, '');
    document.querySelectorAll('.preview-fragment.is-highlighted')
      .forEach((el) => el.classList.remove('is-highlighted'));
    requestAnimationFrame(() => {
      document.querySelectorAll('.preview-fragment').forEach((el) => {
        const uses = (el.dataset.uses || '').split(',').map((s) => s.trim());
        if (uses.includes(short)) el.classList.add('is-highlighted');
      });
    });
    clearTimeout(highlightTimer);
    highlightTimer = setTimeout(() => {
      document.querySelectorAll('.preview-fragment.is-highlighted')
        .forEach((el) => el.classList.remove('is-highlighted'));
    }, 1600);
  }

  // ────────────────────────────────────────────────────────────────────────────
  // TOAST
  // ────────────────────────────────────────────────────────────────────────────
  let toastTimer = null;
  function showToast(msg) {
    toastEl.textContent = msg;
    toastEl.classList.add('is-visible');
    clearTimeout(toastTimer);
    toastTimer = setTimeout(() => toastEl.classList.remove('is-visible'), 1800);
  }

  // ────────────────────────────────────────────────────────────────────────────
  // EXPORT MODAL
  // ────────────────────────────────────────────────────────────────────────────
  const exportModal    = document.querySelector('[data-export-modal]');
  const exportTextarea = document.querySelector('[data-export-textarea]');
  let exportMode = 'full';

  function _buildCss(which) {
    // Exports the ACTIVE mode, wrapped in the selector it ships under:
    //   light → :root { … }    dark → html[data-theme="dark"] { … }
    const sel = activeMode === 'dark' ? 'html[data-theme="dark"]' : ':root';
    const lines = [];
    rows.forEach((row) => {
      const token = row.dataset.token;
      const value = resolved(activeMode, token);
      if (which === 'changed' && value === rowDefault(row)) return;
      lines.push('  ' + token.padEnd(20) + ': ' + value + ';');
    });
    if (which === 'changed' && lines.length === 0) return '/* No changes from default */';
    return sel + ' {\n' + lines.join('\n') + '\n}';
  }

  document.querySelector('[data-export]').addEventListener('click', () => {
    exportTextarea.value = _buildCss(exportMode);
    exportModal.hidden = false;
    exportTextarea.select();
  });

  document.querySelectorAll('[data-export-tab]').forEach((tab) => {
    tab.addEventListener('click', () => {
      exportMode = tab.dataset.exportTab;
      document.querySelectorAll('[data-export-tab]').forEach((t) =>
        t.classList.toggle('is-active', t === tab));
      exportTextarea.value = _buildCss(exportMode);
    });
  });

  document.querySelector('[data-export-close]').addEventListener('click', () => {
    exportModal.hidden = true;
  });

  document.querySelector('[data-export-copy]').addEventListener('click', async () => {
    try { await navigator.clipboard.writeText(exportTextarea.value); }
    catch (_) { exportTextarea.select(); exportTextarea.setSelectionRange(0, 99999); }
    const btn = document.querySelector('[data-export-copy]');
    const orig = btn.textContent;
    btn.textContent = 'Copied!';
    setTimeout(() => { btn.textContent = orig; }, 1400);
  });

  exportModal.addEventListener('click', (e) => {
    if (e.target === exportModal) exportModal.hidden = true;
  });

  // ────────────────────────────────────────────────────────────────────────────
  // HELPERS
  // ────────────────────────────────────────────────────────────────────────────
  function _normaliseHex(v) {
    if (!v) return null;
    v = v.trim();
    if (!v.startsWith('#')) v = '#' + v;
    if (v.length === 4) v = '#' + v[1]+v[1] + v[2]+v[2] + v[3]+v[3];
    if (!/^#[0-9a-fA-F]{6}$/.test(v)) return null;
    return v.toUpperCase();
  }

})();
</script>
</body>
</html>