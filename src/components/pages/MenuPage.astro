---
import BaseLayout    from '../../layouts/BaseLayout.astro';
import MenuEmbed     from '../MenuEmbed.astro';
import IntroPhoto    from '../sections/IntroPhoto.astro';
import { pick, menu as copy, menuPage as pageCopy } from '../../data/i18n';
import { PHOTOS }    from '../../data/photos';
import type { Lang } from '../../data/i18n';

export interface Props { lang: Lang }

const { lang } = Astro.props;
const t        = pick(copy, lang);
const page     = pick(pageCopy, lang);
const title    = page.pageTitle;
const description = page.pageDescription;
---

<BaseLayout title={title} lang={lang} transparentHeader description={description}>
  <IntroPhoto
    lang={lang}
    photo={PHOTOS.menuIntro}
    eyebrow={t.eyebrow}
    heading={t.heading}
    lede={t.lede}
  />

  <section class="section-tight wrap">
    <MenuEmbed lang={lang} />
  </section>
</BaseLayout>
